let a = parseFloat(prompt("Ingresa el valor de A:"));
let b = parseFloat(prompt("Ingresa el valor de B:"));
let c = parseFloat(prompt("Ingresa el valor de C:"));

let resp = (a + 7 + c) / (b + 2 - a) + 2 * b;

alert(`el resultado es: ${resp}`);